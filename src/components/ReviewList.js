import React from 'react';
import './Review';

// export default function ReviewList(props) {
//     //if we want to add a single review to Reviewlist
//     const AddReview = ReviewList.filter((review) => {review.ReviewId === props.ReviewId
// });
//   //if there are 0 reviews
//     if (AddReview.length === 0) {
//       return <p className="text-muted">No Reviews yet</p>;
//     }
//     return (
//         //remember to use a key! Keys are needed!!
//       <>
//         {AddReview.map((review, id) => (
//           <div
//             key={review.id}
//             className="bg-white border rounded shadow p-2 my-2 w-50"
//           >
//             {review.text}
//           </div>
//         ))}
//       </>
//     );
//   }

